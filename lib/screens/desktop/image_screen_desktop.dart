import 'package:flutter/material.dart';
import 'package:reading_diary/models/image_screen_arguments.dart';

class ImageScreenDesktop extends StatelessWidget {
  const ImageScreenDesktop({
    required this.args,
    super.key,
  });

  /// The Arguments passed to this Screen.
  final ImageScreenArguments args;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
